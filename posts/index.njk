---
layout: default.njk
title: Articles
eleventyImport:
  collections: ["post"]
published: Sept 18, 2024
description: How-to articles on various specific problems I've solved.
---

<div class="px-6 max-w-5xl mx-auto">
  <div class="mb-12">
    <h1 class="mb-2">Articles</h1>
    <p class="text-secondary-on-dark">
      How-to articles on various specific problems I've solved.
    </p>
  </div>

  <ul>
    {%- for post in collections.post | reverse -%}
    <li class="my-6">
      <a
        class="shadow-lg hover:shadow-2xl block p-6 border border-solid rounded border-slate-950 hover:border-action-on-dark bg-black bg-opacity-25 fancy-hover bg-action-card-hover-on-dark"
        href="{{ post.url }}"
      >
        {% if post.data.draft %}
        <span
          class="text-xs bg-orange-900 text-white font-bold px-2 py-1 rounded-full inline-block mb-2"
        >
          🚧 Under Construction
        </span>
        {% endif %}
        <h2 class="font-bold text-2xl text-primary-on-dark">
          {{ post.data.title }}
        </h2>
        <p class="text-secondary-on-dark text-sm font-normal my-4">
          {{ post.data.description }}
        </p>

        <div
          class="flex items-baseline justify-between gap-4 flex-wrap sm:flex-nowrap"
        >
          <ul class="flex-1 flex items-end flex-wrap gap-4">
            {%- for tech in post.data.techStack -%}
            <li class="chip">
              {{ tech.name }} {% if tech.version %}v{{tech.version}}{% endif %}
            </li>
            {%- endfor -%}
          </ul>

          <p class="text-secondary-on-dark font-light text-sm">
            Last updated: {{post.date | postDate}}
          </p>
        </div>
      </a>
    </li>
    {%- endfor -%}
  </ul>
</div>
